# .env.example — copy to .env and fill in values
# NEVER commit .env to version control

# A random UUID4 — generate with: python -c "import uuid; print(uuid.uuid4())"
AETHER_KEY=your-uuid4-here

# Server bind (default values shown)
HOST=::
PORT=58008

# Path to project keyword → directory map
PATH_MAP_FILE=path_map.json
